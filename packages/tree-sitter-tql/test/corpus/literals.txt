========================================
string literal
========================================

'hello world'

---

(source_file
  (pipeline
    (pipeline_step
      (expression
        (string_literal
          content: (string_fragment))))))

========================================
string with escape sequences
========================================

'hello\nworld\t\''

---

(source_file
  (pipeline
    (pipeline_step
      (expression
        (string_literal
          content: (string_fragment)
          content: (escape_sequence)
          content: (string_fragment)
          content: (escape_sequence)
          content: (escape_sequence))))))

========================================
regex literal
========================================

/[a-z]+/

---

(source_file
  (pipeline
    (pipeline_step
      (expression
        (regex_literal
          pattern: (regex_fragment))))))

========================================
regex with escapes
========================================

/\d+\.\d+/

---

(source_file
  (pipeline
    (pipeline_step
      (expression
        (regex_literal
          pattern: (regex_escape_sequence)
          pattern: (regex_fragment)
          pattern: (regex_escape_sequence)
          pattern: (regex_escape_sequence)
          pattern: (regex_fragment))))))

========================================
number literal
========================================

42

---

(source_file
  (pipeline
    (pipeline_step
      (expression
        (number_literal)))))
