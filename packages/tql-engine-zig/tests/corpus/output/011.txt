title: a node nested inside a record encodes as a node
grammar: typescript
pending: values

`node` is `Serial`, so it may appear inside a serializable container.

--- tql ---
main = children | is_kind :class_declaration | { at = range, k = kind };

--- source ---
class A {}

--- values ---
[
  {
    "at": {
      "start_byte": 0,
      "end_byte": 10,
      "start_point": { "row": 0, "column": 0 },
      "end_point": { "row": 0, "column": 10 }
    },
    "k": "class_declaration"
  }
]
