title: `filename` yields the empty stream when no path is available
grammar: typescript
pending: values

No `file:` header, so there is no path. Absence is the empty stream, not an
error and not an empty string.

--- tql ---
main = filename;

--- source ---
class A {}

--- values ---
[]
