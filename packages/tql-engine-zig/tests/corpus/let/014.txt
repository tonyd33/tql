title: `let` inside a pipe stage
grammar: typescript
pending: values

The group is evaluated once per input value from the left stage.

--- tql ---
main =
  children | is_kind :class_declaration | let { n = .name | text } in n;

--- source ---
class A {}
class B {}

--- values ---
["A", "B"]
