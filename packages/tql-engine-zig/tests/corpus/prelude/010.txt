title: `contains` searches descendants
grammar: typescript
pending: values

`contains p = exists (descendants | select p)`.

--- tql ---
main = . / :class_declaration | contains (kind = "method_definition");

--- source ---
class A { m() {} }
class B {}

--- values ---
[true, false]
