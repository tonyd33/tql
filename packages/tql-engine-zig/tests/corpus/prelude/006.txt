title: `any` over an empty source is false
grammar: typescript
pending: values

The source yields nothing, so the probe finds no first result.

--- tql ---
main =
  children | is_kind :class_declaration
  | any (.body | children | is_kind :method_definition)
        (\m -> [text m.name ~ r"^get"]);

--- source ---
class B {}

--- values ---
[false]
