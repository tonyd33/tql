title: `within` searches ancestors
grammar: typescript
pending: values

`within p = exists (ancestors | select p)`.

--- tql ---
main = . // :property_identifier | within (kind = "class_declaration");

--- source ---
class A { m() {} }

--- values ---
[true]
