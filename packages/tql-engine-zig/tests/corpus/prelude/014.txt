title: `or_else` yields the primary when it has results
grammar: typescript
pending: values

`or_else primary fallback = branch (probe primary) primary fallback`.

--- tql ---
main =
  children | is_kind :class_declaration
  | or_else (.name | text) (pure "anonymous");

--- source ---
class A {}

--- values ---
["A"]
