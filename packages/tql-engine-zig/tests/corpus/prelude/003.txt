title: `select` passes its input through unchanged
grammar: typescript
pending: values

The output is the node itself, not the predicate's boolean, so the `identity`
arm is what runs on success.

--- tql ---
main =
  children | is_kind :class_declaration
  | select (\c -> [text c.name = "A"]) | kind;

--- source ---
class A {}

--- values ---
["class_declaration"]
