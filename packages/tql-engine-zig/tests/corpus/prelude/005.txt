title: `any` is true when some element satisfies the predicate
grammar: typescript
pending: values

`any source predicate = probe (compose source (select predicate))`, typed
`Filter a b -> Filter b bool -> Filter a bool`.

--- tql ---
main = . / :class_declaration | any (.body / :method_definition) (.name | text ~ r"^get");

--- source ---
class A { getX() {} setY() {} }
class B { setZ() {} }

--- values ---
[true, false]
