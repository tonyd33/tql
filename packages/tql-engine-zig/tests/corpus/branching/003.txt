title: a condition yielding no booleans yields no output
grammar: typescript
pending: values

Neither branch runs. The empty condition stream produces an empty result even
though both branches would succeed.

--- tql ---
main = if (. / :interface_declaration | true) then "yes" else "no";

--- source ---
class A {}

--- values ---
[]
