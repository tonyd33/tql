title: `empty` as a branch yields nothing for that input
grammar: typescript
pending: values

--- tql ---
main = (1, 2, 3) | if . = 2 then . else empty;

--- source ---
class A {}

--- values ---
[2]
