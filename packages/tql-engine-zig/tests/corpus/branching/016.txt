title: `empty` as a branch yields nothing for that input
grammar: typescript
pending: values

`,` is union over filters, so each literal is written `pure n` to be one. The
composed filter is then applied to an ignored input.

--- tql ---
main =
  (pure 1, pure 2, pure 3) | branch (\n -> [n = 2]) identity empty;

--- source ---
class A {}

--- values ---
[2]
