title: both branches run on the same current input, not on the condition
grammar: typescript
pending: values

The condition projects the name, but the selected branch still receives the
class node, so it can navigate again.

--- tql ---
main = . / :class_declaration | if .name | text = "A" then kind else "other";

--- source ---
class A {}
class B {}

--- values ---
["class_declaration", "other"]
