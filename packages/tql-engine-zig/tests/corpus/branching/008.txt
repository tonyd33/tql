title: `and` propagates a multi-output right operand
grammar: typescript
pending: values

The left `true` contributes the whole right stream; the left `false`
contributes exactly one `false`.

--- tql ---
main = (true, false) and (true, false);

--- source ---
class A {}

--- values ---
[true, false, false]
