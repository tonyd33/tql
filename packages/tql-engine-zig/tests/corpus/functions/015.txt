title: mutually recursive globals are one SCC
grammar: typescript
pending: values

--- tql ---
is_even n = if n = 0 then true else is_odd (n - 1);
is_odd n = if n = 0 then false else is_even (n - 1);
main = pure (is_even 4), pure (is_odd 4);

--- source ---
class A {}

--- values ---
[true, false]
