title: partial application yields a function value
grammar: typescript
asserts: core
pending: values

`add 10` is applied to two different arguments, so the partial application was
a reusable value rather than an error.

--- tql ---
add x y = x + y;
main =
  let {
    add10 = add 10
  } in
    pure (add10 1), pure (add10 2);

--- source ---
class A {}

--- values ---
[11, 12]

--- core ---
add = \x -> \y -> op[+] x y
main = letrec { add10 = add 10 } in union (pure (add10 1)) (pure (add10 2))
