title: application binds tighter than pipe
grammar: typescript
pending: values

`. / :class_declaration | name_of .` groups as
`. / :class_declaration | (name_of .)`.

--- tql ---
name_of n = (.name | text) n;
main = children | is_kind :class_declaration | name_of;

--- source ---
class A {}
class B {}

--- values ---
["A", "B"]
