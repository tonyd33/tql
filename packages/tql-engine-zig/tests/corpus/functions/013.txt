title: a recursive global definition
grammar: typescript
pending: values

--- tql ---
countdown n = if n <= 0 then 0 else countdown (n - 1);
main = pure (countdown 5);

--- source ---
class A {}

--- values ---
[0]
