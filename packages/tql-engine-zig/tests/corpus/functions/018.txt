title: a lambda used directly as a pipe stage argument
grammar: typescript
pending: values

--- tql ---
apply f x = f x;
main = pure (apply (\n -> n * 3) 5);

--- source ---
class A {}

--- values ---
[15]
