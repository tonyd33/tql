title: `parent` yields exactly one node below the root
grammar: typescript
pending: values

--- tql ---
main = . // :method_definition | parent | kind;

--- source ---
class A { m() {} }

--- values ---
["class_body"]
