title: static child navigation keeps only matching kinds
grammar: typescript
asserts: core
pending: values

--- tql ---
main = children | is_kind :class_declaration | .name | text;

--- source ---
class A {}
function f() {}
class B {}

--- values ---
["A", "B"]

--- core ---
main = compose (compose (compose children is_kind[class_declaration]) field[name]) text
