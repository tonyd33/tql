title: both operands run on the same current input
grammar: typescript
pending: values

Each operand independently navigates from the same class node.

--- tql ---
main =
  children | is_kind :class_declaration
  | \c -> text c.name = text c;

--- source ---
class A {}

--- values ---
[true]
