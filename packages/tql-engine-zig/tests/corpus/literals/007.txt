title: a raw literal needs no escape for a slash
grammar: typescript
pending: values

`r"a/b"` is the three-character pattern `a/b`. A `/` is not special inside the
literal, so it needs no escape; only `"` closes it.

--- tql ---
main = . // :string | text ~ r"a/b";

--- source ---
let p = "xa/by";

--- values ---
[true]
