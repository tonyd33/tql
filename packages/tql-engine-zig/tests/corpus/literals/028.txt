title: `text`, `kind`, and `range` project a node
grammar: typescript
pending: values

--- tql ---
main = children | is_kind :class_declaration | { k = kind, t = text };

--- source ---
class A {}

--- values ---
[{ "k": "class_declaration", "t": "class A {}" }]
