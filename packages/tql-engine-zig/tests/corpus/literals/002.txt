title: bool literals in filter position
grammar: typescript
asserts: core
pending: values

Union requires one element type, so this fixture unions bools only. LIT-01
covers `int` and REGEX-05 covers a `string`-producing program.

--- tql ---
main = (pure true, pure false, pure true);

--- source ---
class A {}

--- values ---
[true, false, true]

--- core ---
main = union (union (pure true) (pure false)) (pure true)
