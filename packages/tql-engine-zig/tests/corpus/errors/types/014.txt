title: a node is not a condition
grammar: typescript

A non-empty navigation result is not implicitly true.

--- tql ---
main = if children | is_kind :class_declaration then pure "yes" else pure "no";

--- error ---
category: type-mismatch
span: 1:11-1:48
The condition. Expected `Filter node bool`, found `Filter node node`.
