title: `unnest` inverts collection
grammar: typescript
pending: values

--- tql ---
main = [children | is_kind :class_declaration | .name | text] | unnest;

--- source ---
class A {}
class B {}

--- values ---
["A", "B"]
