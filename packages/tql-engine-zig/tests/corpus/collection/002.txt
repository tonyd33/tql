title: `[]` is an empty list, not an empty query
grammar: typescript
pending: values

`[]` yields one value. `empty` yields none. The distinction is the rule.

--- tql ---
main = pure ([] | length);

--- source ---
class A {}

--- values ---
[0]
