title: a global is forced at most once and memoized
grammar: typescript
pending: values

`shared` is referenced twice. Memoization is not directly observable as a
value, so the evidence here is only that both references agree; the timing
claim is carried by CAL-ENV-5 as a `doc` rule.

--- tql ---
shared = 6 * 7;
main = shared, shared;

--- source ---
class A {}

--- values ---
[42, 42]
