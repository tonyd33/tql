title: a `do` block with no statements is permitted
grammar: typescript
pending: values

It simply evaluates its final expression against the current input.

--- tql ---
main root =
  do {
    (children | is_kind :class_declaration | .name | text) root
  };

--- source ---
class A {}

--- values ---
["A"]
