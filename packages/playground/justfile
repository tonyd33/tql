# https://just.systems
# vim: noexpandtab tabstop=4 shiftwidth=4

set shell := ["bash", "-euo", "pipefail", "-c"]

default:
	@just --list

build:
	pnpm run build

serve: build
	python -m http.server 8000 -d build

dev:
	pnpm run dev

clean:
	rm -rf node_modules build static/tql.wasm

