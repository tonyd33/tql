# https://just.systems
# vim: noexpandtab

set shell := ["bash", "-euo", "pipefail", "-c"]

default:
	@just --list

serve:
	python -m http.server 8000 -d public/

build:
	pnpm exec rolldown src/tql.ts --file public/tql.js
